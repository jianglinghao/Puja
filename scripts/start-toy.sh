#!/bin/bash
echo "====================================================="
echo "🧸 正在启动毛绒玩具智能伴侣 Node.js 后端守护进程..."
echo "请确保那台闲置电脑上已经成功开启了本地的 GPT-SoVITS 语音合成服务。"
echo "====================================================="

cd "$(dirname "$0")/../src"

while true
do
    # 启动 Node.js 主服务
    pnpm start
    
    echo "⚠️ 主服务非正常退出，3 秒后将自动重启..."
    sleep 3
done
